export interface PhoneFormValues {
  countryCode: string;
  phoneNumber: string;
}

// Define el tipo para los valores del formulario de datos de usuario
export interface UserDataFormValues {
  names: string;
  fatherLastName: string;
  motherLastName: string;
  address: string;
  postalCode: string;
  state: string;
  city: string;
  email: string;
  password: string;
}